package se.arbetsformedlingen.elin.rapport.util;

import org.apache.commons.lang3.StringUtils;

import se.arbetsformedlingen.utils.log.Logger;
import se.arbetsformedlingen.utils.log.LoggerFactory;

import se.arbetsformedlingen.elin.rapport.util.SomethingElse
import java.lang.reflect.Field;
import java.text.SimpleDateFormat;
import java.util.*;

import static java.lang.String.format;

public class ExcelCSVSkrivare<T> {
    private static final Logger LOGGER = LoggerFactory.getLogger(ExcelCSVSkrivare.class);

    private static final String RADBRYTNING = format("%n");
    private static final String DATEFORMAT = "yyyy-MM-dd";
    private static final String SEPARATOR = ";";

bla 
bla 
bla

import some.stupid.imports.*;
class ThisIsInnerClass {
    
}
package this.is.plain.wrong;
}
